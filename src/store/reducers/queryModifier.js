import initialState from "../initialState";

function queryModifier(state = initialState.queryModifier, action){
    switch (action.type){
        default: return state;
    }
}

export default queryModifier;